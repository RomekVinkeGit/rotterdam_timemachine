Prompt:

Please generate a README.md file for this project. The README should include the following sections:

Project Overview: A brief summary of what the project does.

Repository Structure: A breakdown of the folders and key files in the repository, especially under the src directory, explaining their purpose.

Getting Started: Clear step-by-step instructions on how to:

Set up the environment (e.g., Python version, virtualenv, dependencies).

Install required packages (include commands like pip install -r requirements.txt if applicable).

Run the program.

OpenAI API Key Setup:

Explain how to sign up at https://platform.openai.com to get an API key.

Instruct the user to create a .env file (or specify the method your code uses) and store the API key using a variable like OPENAI_API_KEY=your-key-here.

Usage Instructions: How to use the application after setup, including any CLI commands or UI navigation tips.

Screenshot:

Include a placeholder for a screenshot image with Markdown syntax (e.g., ![App Screenshot](path/to/screenshot.png)).

I will attach the screenshot separately.